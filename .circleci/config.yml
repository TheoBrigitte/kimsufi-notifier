version: 2
jobs:
  run:
    machine:
      - image: ubuntu-2204:current
    steps:
      - checkout
      - run: DATACENTERS=fr,gra,rbx,sbg PLAN_CODE=22sk010 ./hack/check.sh

workflows:
  version: 2
  build:
    jobs:
      - run:
         filters:
          branches:
            only: master
         context:
         - opsgenie
  run:
    triggers:
      - schedule:
          cron: "0 * * * *"
          filters:
            branches:
              only:
                - master
    jobs:
      - run
